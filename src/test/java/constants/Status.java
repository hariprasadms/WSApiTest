package constants;

// This is to show constants can store and use these values where ever is required the tests
// This is not in use. This is for the demo purpose.
public enum Status {
    SUCCESS, NOT_SUCCESS,
}
